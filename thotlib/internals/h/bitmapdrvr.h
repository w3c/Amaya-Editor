/* BitmapDrvr.h -- Definition  X11 bitmap image driver */


#ifndef BitmapDrvr_h
#define BitmapDrvr_h

#define BitmapName "Bitmap (.xbm)" /* ok ? or something else ? We need this
                               to name the format at the UI level... */

#endif
