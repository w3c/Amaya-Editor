void            TbCreation(/* PtrElement pEl, PtrDocument pDoc */);

boolean         TbSelection(/* PtrElement pEl, PtrDocument pDoc, boolean Ext */);

boolean         TbDernierSauve(/* PtrElement pElSv */);

void            TbColleVoisin(/* PtrElement pColle, PtrElement *pElSv, PtrDocument pDoc */);

boolean         TbAttributRef(/* PtrAttribute pAttr */);
