APPLICATION Draw3;

{ message pour les elements :

	- dans la section DEFAULT, ils sont envoyes des qu un elt qcq 
			    	   subit la cmde d edition
	- dans la section ELEMENTS, ils sont associes a un type d element }



DEFAULT
	BEGIN
		ElemSelect.Pre ->	DrawSelectPre;
		ElemNew.Post -> 	CreateAttributes;
		ElemDelete.Pre -> 	DeleteAnElement;
	END;


ELEMENTS

	Draw3 :
		BEGIN
		{	ElemRead.Post -> NewDraw;}
			ElemNew.Post ->  NewDraw;
			ElemDelete.Post -> DeleteDraw;
		END;
	
	GRAPHICS_UNIT :
		BEGIN
			ElemNew.Pre -> CreerUneForme;
			ElemGraphModify.Pre -> GraphModify;
			ElemActivate.Post -> ModifAttributsRectangle;
		END;

	PICTURE_UNIT :
		BEGIN
			ElemNew.Pre -> CreerUneForme;
			ElemGraphModify.Pre -> GraphModify;
			ElemActivate.Post -> ModifAttributsRectangle;
		END;

	Rectangle :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Ellipse :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Polygone :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Trait :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Texte :
		BEGIN
			PRuleModify.Post -> ModifyPRule;
		END;

  	LienSimple :
		BEGIN
			ElemNew.Post -> CreerUnLien;
			ElemDelete.Pre -> DeleteALien;
		END;

  	LienComposite :
		BEGIN
			ElemNew.Post -> CreerUnLien;
		END;

  	PartieOrigineLien :
		BEGIN
			ElemNew.Post -> CreerUnLien;
			ElemDelete.Pre -> DeleteALien;
		END;

  	PartieDestinationLien :
		BEGIN
			ElemNew.Post -> CreerUnLien;
			ElemDelete.Pre -> DeleteALien;
		END;

	FlecheCreuse :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Fleche :
		BEGIN
			ElemNew.Post -> CreerUneForme;
		END;

	Groupe :
		BEGIN
			ElemActivate.Post -> ModifAttributsRectangle;
		END;

ATTRIBUTES
  
   Hauteur:
	BEGIN
		AttrModify.Pre -> ModifyAttribute;
		AttrModify.Post -> ModifyAttributeDim;
	END;

   Largeur:
	BEGIN
		AttrModify.Pre -> ModifyAttribute;
		AttrModify.Post ->  ModifyAttributeDim;
	END;

   PositionV:
	BEGIN
		AttrModify.Pre -> ModifyAttribute;
		AttrModify.Post ->  ModifyAttributePos;
	END;

   PositionH:
	BEGIN
		AttrModify.Pre -> ModifyAttribute;
		AttrModify.Post ->  ModifyAttributePos;
	END;


END
