<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.1 plus MathML 2.0 plus SVG 1.1//EN"
       "http://www.w3.org/2002/04/xhtml-math-svg/xhtml-math-svg.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
  <title>Amaya - Guide utilisateur</title>
  <style type="text/css">

.xml {color: #E50000}
.exercice {color: #A9B200}
h1 {color: #FFFFFF; background-color: #7995B6; padding: 5px 1px}
address {margin-bottom: 30px}
body {background-color: #F9F7F4; color: #666666; font-family: helvetica; }
.author {font-style: italic; font-weight: bold}
.head {margin: 1em 0 3em 0}
h1 {font-size: 24pt}
body {padding-left: 2em}</style>
  <meta name="generator"
  content="amaya 9.0-pre, see http://www.w3.org/Amaya/" />
</head>

<body xml:lang="fr">

<div class="head">
<h1 id="Amaya">Amaya - Guide utilisateur</h1>

<p class="head"><span class="author">Irène Vatton</span> <br />
Dernière mise à jour : <!--$date=-->mercredi 08 dÃ©cembre 2004 - 12:59:45<!--$-->
</p>
</div>

<p>Ce document a pour but d'aider les nouveaux utilisateurs d'Amaya. Pour
plus de détail sur l'interface, il faut se reporter au <a
href="Manual.html.fr">manuel de référence</a>.</p>

<div>
<h3>Plan</h3>
<ol>
  <li><a href="#L85">Introduction</a>
    <ul>
      <li>Pourquoi un éditeur structuré pour HTML ?</li>
      <li>Pourquoi des familles HTML ?</li>
      <li>Les familles HTML</li>
      <li>Quelques règles d'accessibilité à suivre</li>
    </ul>
  </li>
  <li><a href="#L271">Présentation générale d'Amaya</a></li>
  <li><a href="#L483">Paramétrage de l'application</a></li>
  <li><a href="#L608">Création/Edition d'une page Web</a></li>
  <li><a href="#L662">Création du contenu (paragraphes, en-têtes de sections,
    divisions)</a></li>
  <li><a href="#L831">Le listes</a></li>
  <li><a href="#L1002">Les liens</a></li>
  <li><a href="#L11631">Mises en évidence du texte</a></li>
  <li><a href="#L12061">Les tables</a></li>
  <li><a href="#L12281">Recherche/Remplacement - Correction
  orthographique</a></li>
  <li><a href="#L1230">Attributs</a></li>
  <li><a href="#L1232">Création de feuilles de style CSS</a></li>
  <li><a href="#L1242">Détection des erreurs</a></li>
</ol>
</div>

<div>
<h2 id="L85">1. Introduction</h2>

<p>L'objectif du Web est de permettre l'échange d'informations quelque soit
le support.</p>
<ul>
  <li>A opposer à l'approche Word :
    <p>En général la description d'un document Word se limite à la
    présentation, essentiellement l'usage de fontes. Si le destinataire ne
    dispose pas des mêmes fontes, le document peut être illisible.</p>
  </li>
  <li>La solution préconisée est d'utiliser un modèle de description
    clairement défini et ouvert. C'est ce que permet de faire HTML en
    définissant un modèle de document Web standard.
    <p>Avec les CSS (Cascading Style Sheets) il y a même séparation entre la
    structure + contenu et la présentation.</p>
    <p></p>
  </li>
</ul>

<p>L'auteur d'un document Web doit distinguer ce qui est du ressort de la
structure (titres, paragraphes, listes, etc.) de ce qui n'est que de la
présentation (taille de caractères, couleurs, etc.)</p>

<p>Un bon exercice : comprendre un document en lisant ses balises HTML et le
texte.</p>

<h3>Pourquoi un éditeur structuré pour HTML ?</h3>

<p>HTML définit une structure (organisation) de document.</p>
<ul>
  <li><strong>Elements</strong> : Découpage sémantique du document même si ce
    découpage a un effet sur la présentation
    <p>Par exemple un H2 a un effet de présentation mais donne aussi une
    information sémantique. C'est un en-tête moins important qu'un H1 et plus
    important qu'un H3.</p>
    <p>De même un lien (ancre + href) définit plus qu'un simple texte
    coloré/souligné.</p>
  </li>
  <li><strong>Attributs</strong> : Caractérisation sémantique
    <p>L'attribut class="section" sur une division, la cible d'un lien
    href="http://www.w3.org/"</p>
  </li>
</ul>

<p>La décoration des documents est confiée à un langage de style CSS
(Cascading Style Sheets).</p>

<h3>Pourquoi des familles HTML ?</h3>

<p>Avant que le langage CSS ne soit finalisé, HTML avait introduit des
éléments et des attributs qui n'avaient pas de valeur sémantique, mais
servaient à modifier la présentation des documents. Par exemple
&lt;font&gt;...&lt;/font&gt; pour choisir une police de caractères
particulière. Avec le langage CSS, ces éléments et attributs n'ont plus de
raison d'être. La sous-famille de HTML 4.0 Strict élimine les éléments et
attributs HTML qui doivent être remplacé par des propriétés CSS (center,
font, color, background).</p>

<p>La sous-famille de HTML 4.0 Frameset isole les éléments et attributs qui
décrivent le découpage en fenêtres (frames). Le W3C décommande l'utilisation
des "frames" pour faire que les sites Web soient accessibles aux handicapés
(problèmes de navigation et de contexte).</p>

<p>D'autres familles XHTML ont été introduites par :</p>
<ul>
  <li>Besoin de <strong>modularisation</strong> pour s'adapter à de nouveaux
    supports (PDA, téléphones, télévision)</li>
  <li>Nécessité d'avoir des <strong>structures propres</strong> pour filtrer
    et transformer de façon automatique les documents
    <p>Exemple : extraire les titres et ne présenter le texte qui suit qu'à
    la demande</p>
  </li>
  <li>Autoriser la <strong>composition</strong> avec d'autres langages XML
    (RDF, MathML, SVG, SMIL, autres).</li>
</ul>

<h3>Les différences entre HTML et XHTML</h3>
<pre><span class="xml">&lt;?xml version="1.0" encoding="iso-8859-1"?&gt;</span>
<span class="xml">&lt;!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.1//EN"
      "http://www.w3.org/TR/xhtml11/DTD/xhtml11.dtd"&gt;</span>
<span style="color: #9969FF">&lt;!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN"
      "http://www.w3.org/TR/html4/loose.dtd"&gt;</span>
&lt;html <span class="xml">xmlns="http://www.w3.org/1999/xhtml"</span>&gt;
&lt;head&gt;
&lt;meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1"<span style="color: #E50000" class="xml">/</span>&gt;
&lt;title&gt;Amaya - Guide utilisateur&lt;/title&gt;
&lt;/head&gt;
&lt;body <span class="xml">xml:lang="fr"</span>&gt;
&lt;h1 id=<span class="xml">"</span>Amaya<span class="xml">"</span>&gt;Amaya - Guide utilisateur<span class="xml">&lt;/h1&gt;</span>
&lt;h2&gt;Plan<span class="xml">&lt;/h2&gt;</span>
&lt;ol&gt;
&lt;li&gt;Introduction<span class="xml">&lt;/li&gt;</span>
&lt;li&gt;HTML<span class="xml">&lt;/li&gt;</span>
&lt;li&gt;Quelques règles<span class="xml">&lt;/li&gt;</span>&lt;/ol&gt;
<span class="xml">&lt;/body&gt;</span>
<span class="xml">&lt;/html&gt;</span></pre>

<h3>Les familles HTML</h3>

<table border="1">
  <caption></caption>
  <tbody>
    <tr>
      <th>Famille</th>
      <th>Eléments + attributs</th>
      <th>Strucutre</th>
    </tr>
    <tr>
      <td><p>HTML 4.0 Transitional</p>
      </td>
      <td>Tout sauf les frames</td>
      <td>Structure implicite</td>
    </tr>
    <tr>
      <td>HTML 4.0 Strict</td>
      <td>Sans éléments de présentation</td>
      <td>Structure implicite</td>
    </tr>
    <tr>
      <td>HTML 4.0 Frameset</td>
      <td>Les frames</td>
      <td>Structure implicite</td>
    </tr>
    <tr>
      <td>XHTML 1.0 Transitional</td>
      <td>Tout sauf les frames</td>
      <td>Structure bien formée</td>
    </tr>
    <tr>
      <td>XHTML 1.0 Strict</td>
      <td>Sans éléments de présentation</td>
      <td>Structure bien formée</td>
    </tr>
    <tr>
      <td>XHTML 1.0 Basic</td>
      <td>Adapté aux téléphones</td>
      <td>Structure bien formée</td>
    </tr>
    <tr>
      <td>XHTML 1.1</td>
      <td>XHTML 1.0 Strict + annotations Ruby</td>
      <td>Structure bien formée</td>
    </tr>
  </tbody>
</table>

<h3>Quelques règles d'accessibilité à suivre</h3>

<p>Enoncé de quelques règles</p>
<ul>
  <li>Produire des documents valides</li>
  <li>Séparer structure et présentation
    <p>Ne pas considérer que tout le monde dispose du même écran que vous</p>
  </li>
  <li>Ne pas utiliser du JavaScript pour naviguer</li>
  <li>Ne pas utiliser les frames</li>
  <li>Fournir des textes alternés pour les images, les vidéos, etc.</li>
</ul>
</div>

<div>
<h2 id="L271">2. Présentation générale d'Amaya</h2>

<p>Amaya est à la fois navigateur et un éditeur :</p>

<p><strong>Amaya</strong> est un éditeur doté de capacités de navigation.
C'est à dire que pendant que l'utilisateur travaille sur les documents de son
site Web, il peut accéder n'importe quel autre document Web de sites
extérieurs.</p>

<p>Lorsqu'il est utilisé comme navigateur, Amaya se comporte comme beaucoup
d'autres navigateurs Web, excepté pour ce qui est d'activer les liens. Par
défaut le simple clic est utilisé pour poser la sélection (définir le point
d'insertion de texte) et il faut effectuer un double-clic sur un lien pour
l'activer.</p>

<p></p>

<p>Fenêtre initiale :</p>

<p>Par défaut, l'utilisateur voit la vue formatée du document. C'est l'aspect
final du document tel qu'il est vu par un navigateur Web</p>

<p>La barre de menu :</p>
<ul>
  <li>Fichier : création de documents, navigation, impression, fermeture.</li>
  <li>Edition : édition de base, recherche/remplacement, correction
    orthographique, paramétrage.</li>
  <li>XHTML : création/remplacement d'éléments (X)HTML.</li>
  <li>XML : création d'éléments XML.</li>
  <li>Liens : création et destruction des liens.</li>
  <li>Vues : gestion des différentes vues du document.</li>
  <li>Style : gestion du style CSS.</li>
  <li>Annotations : création, destruction d'annotations.</li>
  <li>Aide : donne accès au manuel en ligne.</li>
</ul>

<p>Les barres d'outils</p>

<p>Contient quelques boutons d'accès aux fonctions les plus utilisées</p>
<ul>
  <li>Stop <img alt="(Stop)" src="../images/stop.gif" /> : arrête le
    transfert d'information depuis un serveur Web</li>
  <li>Document précédent <img alt="(Précédent)" src="../images/Back.gif" /> :
    retour au document précédent (s'il y en a un)</li>
  <li>Document suivant <img alt="(Suivant)" src="../images/Forward.gif" /> :
    retour au document suivant (s'il y en a un)</li>
  <li>Recharger<img alt="(Recharger)" src="../images/Reload.gif" /> :
    recharge le document (accès serveur)</li>
  <li>Page personnelle <img alt="(Home)" src="../images/home.gif" /> :
    affiche la page de démarrage</li>
  <li>Sauver <img alt="(Sauver)" src="../images/floppy.gif" /> : sauver les
    modifications</li>
  <li>Imprimer <img alt="(Imprimer)" src="../images/Print.gif" /> : imprime
    le document</li>
  <li>Rechercher/Remplacer <img alt="(Rechercher)" src="../images/find.gif"
    /> : affiche un formulaire de recherche/remplacement de texte</li>
  <li>Mise en évidence <img alt="(Evidence)" src="../images/Em.gif" /> :
    permet de créer/supprimer un élément "mise en évidence"
    (&lt;em&gt;...&lt;/em&gt;)</li>
  <li>Important <img alt="(Important)" src="../images/Strong.gif" /> : permet
    de créer un élément "important" (&lt;strong&gt;...&lt;/strong&gt;)</li>
  <li>Fonte fixe <img alt="(Code)" src="../images/code.gif" /> : permet de
    créer un élément "fonte fixe" (&lt;code&gt;...&lt;/code&gt;)</li>
  <li>Ajout d'une image <img alt="(Image)" src="../images/Image.gif" /> :
    permet de créer un élément "image" (&lt;img/&gt;)</li>
  <li>Titre niveau 1 <img alt="(H1)" src="../images/H1.gif" /> : permet de
    créer un élément "titre niveau 1" (&lt;h1&gt;...&lt;/h1&gt;)</li>
  <li>Titre niveau 2 <img alt="(H2)" src="../images/H2.gif" /> : permet de
    créer un élément "titre niveau 2" (&lt;h2&gt;...&lt;/h2&gt;)</li>
  <li>Titre niveau 3 <img alt="(H3)" src="../images/H3.gif" /> : permet de
    créer un élément "titre niveau 3" (&lt;h3&gt;...&lt;/h3&gt;)</li>
  <li>Liste <img alt="(Liste)" src="../images/OList.gif" /> : permet de créer
    un élément "liste à puces" (&lt;ul&gt;...&lt;/ul&gt;)</li>
  <li>Liste numérotée <img alt="(Liste num)" src="../images/NumList.gif" /> :
    permet de créer un élément "liste numérotée"
  (&lt;ol&gt;...&lt;/ol&gt;)</li>
  <li>Liste de définitions <img alt="(Définitions)" src="../images/DL.gif"
    /> : permet de créer un élément "liste de définition"
    (&lt;dl&gt;...&lt;/dl&gt;)</li>
  <li>Lien <img alt="(Lien)" src="../images/Link.gif" /> : permet de créer un
    élément "titre niveau 1" (&lt;a href="..."&gt;...&lt;/a&gt;)</li>
  <li>Table <img alt="(Table)" src="../images/table.gif" /> : permet de créer
    un élément "titre niveau 1" (&lt;table&gt;...&lt;/table&gt;)</li>
  <li>Maths <img alt="(Math)" src="../images/Math.gif" /> : palette pour
    créer des formules</li>
  <li>Graphique <img alt="(SVG)" src="../images/Graph.gif" /> : palette pour
    créer des graphiques vectoriels</li>
  <li>Bibliothèque SVG <img alt="(Librairie)" src="../images/libsvg.gif" /> :
    affiche la palette de graphiques prédéfinis</li>
  <li>Ligne de temps <img alt="(Animation)" src="../images/anim.gif"
    /> : affiche la ligne de temps pour l'animation des graphiques</li>
</ul>

<p>et une zone d'affichage de l'adresse du document.</p>
</div>

<div>
<h2 id="L483">3. Paramétrage de l'application</h2>

<p>Avant d'éditer des documents Web avec Amaya, il est important de fixer un
certain nombre de paramètres. Pour cela il faut ouvrir au moins deux des dix
menus de préférences disponibles dans <strong>Éditer/Préférences</strong>.
Chaque menu présente trois boutons d'action :</p>

<table border="0" cellspacing="5" cellpadding="5"
summary="action button chart">
  <tbody>
    <tr>
      <th>Appliquer</th>
      <td>Ce bouton valide et stocke les options affichées dans le menu.</td>
    </tr>
    <tr>
      <th>Défauts</th>
      <td>Ce bouton rétablit, mais ne sauve pas, les valeurs par défaut. Il
        faut utiliser le bouton Appliquer pour valider les options.</td>
    </tr>
    <tr>
      <th>Terminer</th>
      <td>Ce bouton ferme le menu.</td>
    </tr>
  </tbody>
</table>

<p>Voici les paramètres importants à régler :</p>

<h4>Dans les préférences générales</h4>
<ul>
  <li><strong>Page de démarrage</strong> : Spécifie l'URI qu'Amaya chargera
    au démarrage ou quand l'utilisateur pressera le bouton <img
    alt="(Page personelle)" src="../images/home.gif" /> (par exemple l'URI
    <code>http://www.w3.org</code>).</li>
  <li><strong><a id="Enable">Activer le coller ligne à ligne</a></strong> :
    Cette option concerne le coller depuis une autre application. Quand cette
    option est activée, les lignes de l'application source sont conservées.
    Sinon les caractères « fin de ligne » sont remplacés par des espaces.</li>
  <li><strong>Produire des fichiers de sauvegarde</strong> : Si cette option
    est active Amaya va produire périodiquement un fichier de sauvegarde pour
    chaque document édité.</li>
  <li><strong>Montrer boutons</strong> : Contrôle l'affichage de la barre de
    boutons pour tous les documents.</li>
  <li><strong>Montrer l'adresse</strong> : Contrôle si Amaya affiche
    l'adresse des documents.</li>
  <li><strong>Montrer les cibles</strong> : Contrôle si les cibles de liens
    sont visualisées ou non. Montrer les cibles peut aider quand on veut
    créer de nouveaux liens. Cette option agit immédiatement sur tous les
    documents édités.<br />
  </li>
  <li><strong>Zoom caractères</strong> : Cette option vous permet de réduire
    ou d'augmenter les tailles des polices de caractères utilisées. Cette
    option a un effet immédiat sur tous les documents édités. <br />
  </li>
  <li><strong>Langue du dialogue</strong> : Spécifie la langue utilisée dans
    les menus et les messages. La langue par défaut est l'anglais américain
    (valeur en). D'autres langues sont possibles comme le français (fr),
    l'italien (it), l'espagnol (es), et l'allemand (de).</li>
</ul>

<h4>Dans les préférences de navigation</h4>
<ul>
  <li><strong>Charger les images</strong> : Contrôle si les images doivent
    être chargées et affichées. Sinon, l'attribut ALT est visualisé à la
    place de l'image. Cette option prend effet dès que le formulaire est
    validé. <br />
  </li>
  <li><strong>Charger les objets</strong> : Contrôle si les objets doivent
    être chargés et affichés. Sinon, le contenu de l'objet est visualisé à la
    place de l'objet. Cette option prend effet dès que le formulaire est
    validé.</li>
  <li><strong>Montrer les images de fond</strong> : Contrôle si les images en
    arrière plan doivent être montrées ou non. Cette option prend effet dès
    que le formulaire est validé.</li>
  <li><strong>Le double clic active les liens</strong> : Depuis la première
    version d'Amaya, les utilisateurs ont besoin de double cliquer sur un
    lien pour l'activer. Cela permet de différencier l'édition d'un lien de
    son activation. Vous pouvez changer ce comportement en désactivant cette
    option.<br />
  </li>
</ul>

<p><a href="Configure.html.fr">A voir pour plus de détail</a></p>
</div>

<div>
<h2 id="L608">4. Création/Edition d'une page Web</h2>

<p>L'entrée <strong>Fichier/Nouveau</strong> permet de créer un nouveau
document d'un type donné. Amaya demande de spécifier l'URI ou le nom local du
document à créer (un nom par défaut est généré). Après confirmation, Amaya
ouvre un nouveau document vide.</p>

<p>L'entrée <strong>Fichier/Ouvrir</strong> remplace le document affiché.
Amaya utilise la même entrée de menu par le nouveau document sélectionné.
Amaya utilise la même entrée de menu pour ouvrir un document local ou un
document d'un serveur Web.</p>

<p>L'entrée <strong>Fichier/Ouvrir dans nouvelle fenêtre</strong> affiche le
nouveau document sélectionné dans une nouvelle fenêtre.</p>

<p>Les entrées du menu XHTML présentent les éléments que l'on peut insérer
dans le document affiché, en fonction de la famille XHTML choisie. La manière
la plus simple de créer de nouveaux éléments dans un document est de placer
le point d'insertion dans la position voulue et d'utiliser le menu</p>

<p><strong>Xhtml</strong>, ou les boutons correspondant aux éléments les plus
communs (images, en-têtes, listes...). Ces boutons ou entrées de menu créent
l'élément correspondant à la position courante. Si l'élément désiré ne peut
pas être créé exactement à la position choisie. Amaya tente alors de créer
l'élément à l'endroit le plus proche possible.</p>

<h3>Définir le titre du document</h3>

<p>Par défaut Amaya donne le titre "No title" au document nouvellement créé
parce que cet élément est obligatoire. Il ne faut pas oublier de mettre à
jour ce titre en activant l'entrée <strong>XHTML/Changer le
titre</strong>.</p>

<h3>Définir la langue utilisée</h3>

<p>Il est important de définir la langue dans laquelle est écrit le document.
Sans indication explicite, le document est supposé écrit en anglais. La
langue est utilisée par le correcteur orthographique.</p>

<p>Pour définir qu'un document est écrit français, il faut sélectionner
l'élément &lt;body&gt; et lui appliquer l'attribut lang=fr.</p>

<h4>Première méthode</h4>

<p>On pose la sélection n'importe où dans le document et on utilise la touche
F2 sous Windows (Esc sous Linux) pour remonter la sélection sur l'élément
&lt;body&gt;. La barre de statut affiche l'élément sélectionné. A ce moment
là on sélectionne l'entrée du menu <strong>Attributs/Langue</strong> pour
afficher le formulaire de choix de la langue.</p>

<h4>Deuxième méthode</h4>

<p>On ouvre la vue Structure (entrée <strong>Vue/Montrer la
structure</strong>), on sélectionne l'élément &lt;body&gt;.A ce moment là on
sélectionne l'entrée du menu <strong>Attributs/Langue</strong> pour afficher
le formulaire de choix de la langue.</p>
</div>

<div>
<h3><a id="L1104">Sauver des documents en HTML</a></h3>

<p><b>Amaya</b> ne fournit aucune vue d'ensemble des pages installées sur le
serveur. Cependant il est possible de sauver ou copier des fichiers sur un
serveur ou sur sur son disque local grâce aux commandes
<strong>Sauver</strong> et <strong>Sauver comme</strong>. Pendant ces
opérations, Amaya tient compte des images.</p>

<h4><a id="L1517">La commande Sauver</a></h4>

<p>La commande sauver peut être invoquée soit en choisissant
<strong>Sauver</strong> depuis le menu <strong>Fichier</strong> (<code>Ctrl
s</code> pour Windows, <code>Ctrl x Ctrl s</code> pour Unix) ou bien en
cliquant sur le bouton <img alt="floppy" src="../images/floppy.gif" />.</p>

<h4><a id="L1518">La commande Sauver comme</a></h4>

<p>Le fait de sélectionner l'entrée <strong>Sauver comme</strong> dans le
menu <strong>Fichier</strong> ouvre une boîte de dialogue qui permet de
sauver le document courant à une adresse différente (sur un serveur Web ou
sur le disque local) ou de changer certains paramètres, comme le format du
document et l'encodage.</p>

<p style="text-align: center; text-indent: 15pt"><img
src="../images/SaveAs-fr.gif" alt="SaveAs" /></p>

<h2 id="L662">5. Création du contenu (paragraphes, en-têtes de sections,
divisions)</h2>

<p>Par défaut, Amaya crée un nouveau paragraphe chaque fois que l'on active
la touche <code>Enter</code>. Une fois le paragraphe et son contenu créé, on
peut insérer un en-tête de section, avant, après, ou au milieu du
paragraphe,</p>
<ul>
  <li>en activant un des boutons Hn,</li>
  <li>ou en tapant <code>Ctrl n</code> (n allant de 1 à 6),</li>
  <li>ou en sélectionnant, l'entrée correspondante du menu
    <strong>XHTML</strong>.</li>
</ul>

<p>Quand l'insertion se fait au milieu du paragraphe, Amaya coupe le
paragraphe existant en deux et insère l'en-tête au milieu.</p>

<h3>Transformations</h3>

<p>Si une partie du paragraphe est sélectionnée au moment où on insère
l'en-tête, ce texte sera supprimé dans le paragraphe et inséré dans l'en-tête
créé.</p>

<p>On peut aussi sélectionner le paragraphe (touche <code>F2</code> ou
<code>Esc</code>) et transformer ainsi le paragraphe en en-tête. Un ou
plusieurs paragraphes peuvent en fait se transformer en liste, table,
préformaté en fonction de l'élément cible choisi. L'entrée de menu
<strong>Edition/Transformer</strong> propose les cibles possibles pour la
sélection courante.</p>

<h3>Préformaté</h3>

<p>L'élément préformaté est un paragraphe particulier qui permet de préserver
tous les caractères : espace, fin de ligne.</p>

<p>Pour transformer un paragraphe en préformaté, on sélectionne le paragraphe
en entier et on active l'entrée de menu <strong>XHTML/Préformaté</strong> (ou
<code>Ctrl h Ctrl p</code>).</p>

<p>Pour transformer un préformaté en paragraphe, on sélectionne le préformaté
en entier et on active l'entrée de menu <strong>XHTML/Paragraphe</strong>.</p>

<p>Quand la sélection se trouve à la fin d'un &lt;pre&gt;, la touche
<code>Enter</code> crée une nouvelle ligne du &lt;pre&gt;. Pour créer un
paragraphe après le &lt;pre&gt;, il faut sélectionner le &lt;pre&gt; en
utilisant la touche <code>Esc</code> (Linux) ou <code>F2</code> (Windows)
puis <code>Enter</code>.</p>

<h3>Division</h3>

<p>Il est possible de grouper une série de paragraphes, en-têtes, etc. dans
une division. Pour cela, on sélectionne les éléments à grouper ainsi que
l'entrée DIV dans le menu XHTML.</p>

<h3>La touche Enter</h3>

<p>La touche <code>Enter</code> (ou Return) joue un rôle particulier. En
général elle clôt l'élément courant et crée un paragraphe juste après. Si un
élément complet est sélectionné, la touche <code>Enter</code> crée une autre
instance de cet élément. Par exemple si on sélectionne un en-tête, la touche
<code>Enter</code> va créer un en-tête vide après.</p>

<h3><a id="Back">Les touches Back Space et Delete</a></h3>

<p>Des éléments consécutifs peuvent être fusionnés, même s'ils ne sont pas au
même niveau dans la structure du document. La fusion s'effectue à l'aide des
touches <code>Back Space</code> et <code>Delete</code>. Quand le point
d'insertion est à la fin d'un élément, utilisez la touche
<code>Delete</code>. Quand il est au début, utilisez la touche <code>Back
Space</code>. Dans les deux cas, ces touches n'effacent aucun caractère.
Elles fusionnent des éléments séparés par une limite de structure.</p>

<p>Quand le point d'insertion est à l'intérieur d'un élément, ces deux
touches effacent comme d'habitude le caractère suivant ou le caractère
précédent.</p>

<h3>Caractères spéciaux</h3>

<p>Pour les caractères ISO-latin qui ne sont pas directement disponibles
depuis le clavier, Amaya utilise le support standard fournit par le
système.</p>

<p>Amaya fournit aussi des raccourcis pour certains caractères non
disponibles au clavier comme :</p>
<ul>
  <li>Pour obtenir un espace non sécable, vous devez taper <code>Ctrl -
    espace</code>. Ce caractère sera sauvé comme une entité
    (<code>&amp;nbsp;</code>).</li>
  <li>Pour obtenir un &euro; (euro), vous devez taper <code>Alt e</code>.</li>
  <li>Pour obtenir un «, vous devez taper <code>Ctrl &lt;</code> (ou
    <code>Shift</code> <code>Ctrl &lt;</code> selon le clavier).</li>
  <li>Pour obtenir un », vous devez taper <code>Shift Ctrl &gt;</code>.</li>
</ul>

<h3>Insérer une image</h3>

<p>Pour insérer une image, on choisit l'entrée <strong>Image</strong> dans le
menu <strong>XHTML</strong> (raccourci <code>Ctrl h Ctrl i</code>) ou on
clique sur le bouton <img alt="Image " src="../images/Image.gif"
longdesc="longdesc.htm" />. Une boîte de dialogue s'affiche qui permet de
rentrer le nom du fichier et le <a href="ImageMaps.html.fr#Alternative">texte
de remplacement</a> ('ALT' texte) de l'image. L'insertion n'est prise en
compte que lorsque le texte de remplacement est fourni.</p>

<p style="text-align: center"><img alt="(Insertion image)"
src="../images/create_image-fr.png" /></p>

<p>L'image est affichée avec ses dimensions par défaut. On peut
redimensionnerer l'image insérée :</p>
<ul>
  <li>soit par manipulation directe - en combinant la touche
    <kbd>Control</kbd> et le <kbd>bouton droit</kbd> ou le <kbd>bouton du
    milieu</kbd> de la souris,</li>
  <li>soit en éditant les attributs <strong>width</strong> et
    <strong>height</strong> de l'image. Quand l'image est sélectionnée, on
    sélectionne l'entrée <strong>width</strong> ou <strong>height</strong> du
    menu <strong>Attributs</strong>.</li>
</ul>

<p>Par défaut l'image est cadrée à gauche. On peut modifier le cadrage de
l'image sélectionnée en modifiant l'alignement dans le formulaire
<strong>Format</strong> du menu <strong>Style</strong>.</p>

<h3>Remplacer une image</h3>

<p>Pour remplacer une image il faut sélectionner cette image en draguant sur
l'image avec la souris. Procédez alors comme pour l'ajout d'une image (voir
ci-dessus). Lorsqu'une image (et seulement une image) est entièrement
sélectionnée, la boîte de dialogue des images effectue le remplacement de
l'image sélectionnée au lieu d'insérer une nouvelle image.</p>

<h3 style="clear: both">Copier/Coller, Défaire/Refaire</h3>

<p>Amaya permet copier/coller non seulement du texte et des images, mais
aussi une structure complexe comme une division ou une suite de paragraphes
et d'en-têtes.</p>

<p>Il est toujours possible de défaire et de refaire les dernières
modifications opérées dans le document.</p>

<p><a href="Creating.html.fr">A voir pour plus de détail</a></p>

<p class="exercice"><em>Exercice 1 :</em> Créer un document contenant des
paragraphes, des en-têtes, des divisions.</p>

<h2 id="L831">6. Les listes</h2>

<h3>Editer les listes à puces ou les listes numérotées</h3>

<h4>Première méthode</h4>

<p>En cliquant sur <img alt="(Liste)" src="../images/OList.gif" /> (ou
<code>Ctrl h Ctrl l</code>) on crée une liste à puces et un premier item de
liste à l'intérieur.</p>
<ul>
  <li>Il ne reste plus qu'à taper le texte de l'item.
    <p>La touche <code>Enter</code> en fin d'item va créer un paragraphe vide
    dans l'item et on peut immédiatement saisir le contenu de ce nouveau
    paragraphe.</p>
  </li>
  <li>Si on ne veut pas insérer de paragraphe mais passer à l'item suivant,
    on double la touche <code>Enter</code>.
    <p>Attention en cliquant sur <img alt="(Liste)" src="../images/OList.gif"
    /> (ou <code>Ctrl h Ctrl l</code>) on crée une nouvelle liste à puces à
    l'intérieur de l'item courant.</p>
  </li>
  <li>Si on veut terminer la liste, on triple la touche
  <code>Enter</code>.</li>
</ul>

<p>En cliquant sur <img alt="(Liste numérotée)" src="../images/NumList.gif"
/> (ou <code>Ctrl h Ctrl n</code>) on crée une liste numérotée et un item de
liste à l'intérieur.</p>

<h4>Deuxième méthode</h4>

<p>On peut saisir du texte sans définir au préalable de type d'élément. Par
défaut, Amaya crée un nouveau paragraphe chaque fois que l'on active la
touche <code>Enter</code>. On peut ensuite sélectionner plusieurs paragraphes
et cliquer sur <img alt="(Liste)" src="../images/OList.gif" /> (ou <code>Ctrl
h Ctrl l</code>) pour créer une liste à puces.</p>

<p>Pour sélectionner tout le contenu d'une suite de paragraphes, on utilise
la touche <code>F2</code> sous Windows (<code>Esc</code> sous Linux).</p>

<h4>Transformation d'une liste à puces et en liste numérotée</h4>

<p>On pose la sélection dans un item de liste, puis en doublant la touche
<code>F2</code> sous Windows (<code>Esc</code> sous Linux) on sélectionne la
liste complète. En cliquant sur <img alt="(Liste numérotée)"
src="../images/NumList.gif" /> (ou <code>Ctrl h Ctrl n</code>) on crée une
liste numérotée à la place. On peut bien sûr passer de la même façon d'une
liste numérotée à une liste à puces.</p>

<h3 id="edit-dl">Editer les listes de définitions (dl/dt/dd)</h3>

<p>En cliquant sur <img alt="(Liste de définitions)" src="../images/DL.gif"
/> (ou <code>Ctrl h Ctrl d</code>) on crée une liste de définitions.</p>

<p>La touche <code>Enter</code> est également utile pour éditer des listes de
définitions. Considérez l'exemple suivant :</p>

<blockquote>
  <dl>
    <dt>Terme 1</dt>
    <dt>Terme 2</dt>
      <dd>Définition des deux termes.</dd>
  </dl>
</blockquote>

<p>C'est une liste de définitions (<code>dl</code>) contenant deux termes
(<code>dt</code>) suivis par une définition (<code>dd</code>). Pour ajouter
un nouveau paragraphe à l'intérieur de la définition (<code>dd</code>),
placez le point d'insertion à la fin (après le mot "termes") et appuyez sur
la touche <code>Enter</code>. Si vous pressez <code>Enter</code> deux fois,
vous sortez de la définition en cours (<code>dd</code>) et vous créez un
nouveau terme (<code>dt</code>). A la fin de ce nouveau terme, presser une
fois la touche <code>Enter</code> vous permet de créer encore un nouveau
terme (<code>dt</code>). La presser deux fois crée une nouvelle définition
(<code>dd</code>).</p>

<p>Si vous voulez créer un nouveau terme avec sa définition <em>avant</em> le
Terme 1, mettez le point d'insertion au début du terme 1 et appuyez sur
<code>Enter</code>. Tapez le nouveau terme (<code>dt</code>), puis pressez
<code>Enter</code> deux fois : une nouvelle définition (<code>dd</code>) est
créée directement après le nouveau terme.</p>
</div>

<p>De façon générale on utilise la touche <code>Enter</code> pour insérer des
éléments de structure avant ou après des listes ou des items de liste, et les
touches <code>Back Space</code> et <code>Delete</code> pour fusionner des
listes ou des items de listes.</p>

<p class="exercice"><em>Exercice 2 :</em> Créer et transformer des listes.</p>

<div>
<h2 id="L1002">7. Les liens</h2>

<p>Les liens hypertexte permettent de relier des parties de document
entre-elles ou des documents entre-eux.</p>

<h3>Création de cibles</h3>

<p>La cible d'un lien hypertexte peut être</p>
<ul>
  <li>un document entier,</li>
  <li>ou un passage du document englobé par une ancre cible,</li>
  <li>ou un élément HTML qui possède un identificateur (attribut ID)</li>
</ul>

<p>Pour créer une cible dans un document, il suffit de sélectionner le
passage désiré et d'activer l'entrée de menu <strong>Créer cible</strong>
(<code>Ctrl l Ctrl t</code>) dans le menu <strong>Liens</strong>. Si la
sélection porte sur un élément HTML entier, Amaya va lui associer un
identificateur unique. Sinon, Amaya va englober le contenu de la sélection
dans une ancre cible.</p>

<p>On peut demander à Amaya d'associer un identificateur à tous les éléments
d'un certain type dans le document. Pour cela on active l'entrée
<strong>Ajouter/Retirer des attributs ID</strong> dans le menu
<strong>Liens</strong>. Par exemple, on peut demander d'associer un
identificateur à tous les éléments H2.</p>

<h3>Création de liens</h3>

<p>L'origine d'un lien est une ancre qui possède un attribut
<code>href</code>. On procède de la même façon pour un lien vers un document
entier ou une cible.</p>

<h4>Lien vers un autre document édité par Amaya</h4>
<ol>
  <li>On sélectionne le passage choisi.</li>
  <li>On clique sur le bouton <img alt="Link " src="../images/Link.gif"
  /></li>
  <li>On clique sur le document cible affiché.</li>
</ol>

<p>Autre méthode :</p>
<ol>
  <li>On sélectionne le passage choisi.</li>
  <li>On choisit <strong>Créer/changer lien</strong> du <a
    href="Browsing.html.fr"></a>menu <strong>Liens</strong> (raccourci
    <code>Ctrl l Ctrl l</code>) pour afficher le formulaire de création de
    lien.
    <p><img alt="(Création de lien)" src="../images/create_link-fr.png" /></p>
  </li>
  <li>Soit on saisit l'URI du document cible et on confirme. Soit on décide
    de désigner la cible avec la souris (bouton Clic puis clic sur le
    document cible affiché).</li>
</ol>

<p>Dans tous les cas, une fois la cible choisie, Amaya va calculer l'URL
relative qui permet d'aller du document source au document cible.</p>

<h4>Lien vers une ancre cible d'un document édité par Amaya</h4>

<p>Pour créer un lien vers une ancre cible particulière ou vers un élément
cible particulier d'un document édité par Amaya, il faut d'abord demander la
visualisation des cibles <a id="target"><img src="../images/target.gif"
alt=" target " /></a> dans le document concerné en activant l'entrée
<strong>Montrer les cibles</strong> du menu <strong>Vues</strong> ou ouvrant
la vue des liens (entrée <strong>Montrer les liens</strong> du menu
<strong>Vues</strong>). Ensuite :</p>
<ol>
  <li>On sélectionne le passage choisi.</li>
  <li>On clique sur le bouton <img alt="Link " src="../images/Link.gif"
  /></li>
  <li>On clique sur la cible choisie.</li>
</ol>

<h4>Lien vers un autre document non affiché</h4>

<p>Pour créer un lien vers un autre document local ou distant :</p>
<ol>
  <li>On sélectionne le passage choisi.</li>
  <li>On clique sur le bouton <img alt="Link " src="../images/Link.gif"
  /></li>
  <li>On clique sur une zone du document qui ne peut pas être une cible pour
    afficher le formulaire de saisie de l'URL. Si le document est local, on
    peut utiliser le browser de fichier pour sélectionner le fichier.</li>
</ol>

<p>Autre méthode :</p>
<ol>
  <li>On sélectionne le passage choisi.</li>
  <li>On choisit <strong>Créer/changer lien</strong> du menu
    <strong>Liens</strong> (raccourci <code>Ctrl l Ctrl l</code>)</li>
  <li>On saisit l'URL du document cible et on confirme.</li>
</ol>

<h4><a id="Changing">Mettre à jour un lien</a></h4>

<p>Pour garder une ancre mais changer la cible du lien correspondant, on
place la sélection dans l'ancre et on procède comme pour la création de lien
(bouton <img alt="Link " src="../images/Link.gif" /> ou l'entrée
<strong>Créer/changer Lien</strong> du menu <strong>Liens</strong>). On peut
alors désigner la nouvelle cible ou éditer l'URI.</p>

<p>Pour changer la cible d'un lien, on peut également éditer l'attribut
<code>href</code> dans la vue structure.</p>

<h4><a id="Removing">Supprimer un lien ou une ancre cible</a></h4>

<p>Le menu <strong>Liens</strong> contient une entrée <strong>Détruire
ancre</strong> qui permet de détruire l'ancre cible ou le lien sélectionné.
Il suffit que la sélection soit à l'intérieur de l'ancre à détruire. L'ancre
est détruite, mais son contenu reste.</p>

<p class="exercice"><em>Exercice 3 :</em> Créer des liens dans un document et
entre documents, copier/coller de structures contenant des liens, remplacer,
détruire.</p>
</div>

<div>
<h2 id="L11631">8. Mises en évidence du texte</h2>

<p>Il est possible de marquer des parties de texte mises en ligne soit en
utilisant les CSS (voir plus loin), soit en utilisant les éléments mise en
évidence, important, code, citation, abréviation, etc. de HTML.</p>

<p>Les entrées de menu et les boutons correspondants fonctionnent comme des
bascules (activée/désactivées).</p>

<p>Souvent l'introduction de mises en évidence engendrent des problèmes de
perte des blancs.</p>

<h3>Traitement des blancs</h3>

<p>Le traitement des blancs est appliqué par Amaya lors du chargement d'un
document et lors de l'édition de celui-ci. Par exemple, le code source
suivant:</p>
<pre><code>    &lt;p&gt;Amaya supprime les</code>&lt;strong&gt;<code> blancs non significatifs </code>&lt;/strong&gt;<code>au chargement&lt;/p&gt;</code></pre>

<p>donnera :</p>
<pre><code>    Amaya supprime les</code><code><strong>blancs non significatif</strong></code><code><strong>s</strong>au chargement</code></pre>

<p>car les blancs de début et de fin de l'élément <code>strong</code> sont
considérés comme non significatifs et sont donc supprimés.</p>

<p>La forme correcte aurait été :</p>
<pre>    &lt;p&gt;Amaya supprime les &lt;strong&gt;blancs non significatifs&lt;/strong&gt; au chargement&lt;/p&gt;</pre>
</div>

<div>
<h2 id="L12061">9. Les tables</h2>

<h3><a id="Creating1">Créer une nouvelle table</a></h3>

<p>Quand on active le bouton table <img src="../images/table.gif"
alt="Table button" /> ou quand on sélectionne l'entrée <strong>Table</strong>
depuis le menu <strong>Xhtml</strong>, Amaya visualise une boîte de dialogue
qui permet de spécifier le nombre initial de lignes, de colonnes, ainsi que
la bordure en pixels.</p>

<p style="text-align: center"><img alt="(Create table)"
src="../images/create_table-fr.png" /></p>

<p>En confirmant on crée une table comme suit.</p>

<table border="1" summary="table">
  <caption>I</caption>
  <tbody>
    <tr>
      <td></td>
      <td></td>
    </tr>
    <tr>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

<p>L'espace au dessus de la table est réservé à la légende. Le point
d'insertion est automatiquement placé dans cette légende pour en saisir le
contenu. La touche <code>Delete</code> supprime la légende.</p>

<p>La nouvelle table contient des cellules vides. On peut cliquer sur l'une
d'entre elles au hasard et y insérer du texte, des images et autre. On peut
également déplacer le point d'insertion de cellule en cellule par
l'intermédiaire des flèches du clavier.</p>

<h3><a id="Adding3">Insérer/Ajouter une colonne</a></h3>

<p>Il n'existe deux commandes spécifiques pour créer une nouvelle
colonne :</p>
<ul>
  <li><strong>Insérer une colonne</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl b</code>) ajoute une nouvelle
    colonne avant la colonne sélectionnée.</li>
  <li><strong>Ajouter une colonne</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl a</code>)ajoute une nouvelle
    colonne après la colonne sélectionnée.</li>
</ul>

<p>Si on a sélectionné une cellule complète par <code>F2</code> (Windows) ou
<code>Esc</code> (Unix) la touche Enter crée une nouvelle cellule après et la
colonne correspondante.</p>

<h3>Détruire une colonne</h3>

<p>Pour détruire une colonne de table, il faut sélectionner la colonne et
utiliser la commande <code>Delete</code>. Il y a deux méthodes pour
sélectionner une colonne :</p>
<ol>
  <li>Soit l'entrée <strong>Sélectionner la colonne</strong> dans le
    sous-menu <strong>Table</strong> (<code>Ctrl t Ctrl c</code>).</li>
  <li>Soit étendre la sélection sur au moins deux cellules dans la même
    colonne (Cell 1.1 à Cell 2.1 dans l'exemple ci-dessous) puis deux fois
    <code>F2</code> (Windows) ou <code>Esc</code> (Unix) pour sélectionner la
    colonne.</li>
</ol>

<table border="1" summary="table">
  <caption>Légende</caption>
  <tbody>
    <tr>
      <td>Cell 1.1</td>
      <td>I</td>
      <td>Cell 1.2</td>
      <td>Cell 1.3</td>
    </tr>
    <tr>
      <td>Cell 2.1</td>
      <td></td>
      <td></td>
      <td></td>
    </tr>
  </tbody>
</table>

<h3>Copier/Coller ou Déplacer une colonne</h3>

<p>Pour déplacer ou copier/coller une colonne de table :</p>
<ol>
  <li>Il faut sélectionner la colonne comme dans le cas de <strong>Détruire
    une colonne</strong> et copier (<code>Ctrl c</code>) ou couper
    (<code>Ctrl x</code> pour Windows, <code>Ctrl w</code> pour Unix) son
    contenu.</li>
  <li>Pour coller la colonne avant une autre colonne
    <ul>
      <li>soit on sélectionne le début d'une cellule de colonne et on active
        la commande coller (<code>Ctrl v</code> pour Windows, <code>Ctrl
        y</code> pour Unix),</li>
      <li>soit on utilise l'entrée de menu <strong>Coller avant</strong> dans
        le sous-menu <strong>Table</strong> (<code>Ctrl t Ctrl p</code>).</li>
    </ul>
  </li>
  <li>Pour coller la colonne après une autre colonne
    <ul>
      <li>soit on sélectionne la fin d'une cellule de colonne et on active la
        commande coller (<code>Ctrl v</code> pour Windows, <code>Ctrl
        y</code> pour Unix),</li>
      <li>soit on utilise l'entrée de menu <strong>Coller après</strong> dans
        le sous-menu <strong>Table</strong> (<code>Ctrl t Ctrl v</code>).</li>
    </ul>
  </li>
</ol>

<h3><a id="Adding11">Insérer/Ajouter une ligne</a></h3>

<p>Il n'existe deux commandes spécifiques pour créer une nouvelle ligne :</p>
<ul>
  <li><strong>Insérer une ligne</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl i</code>) ajoute une nouvelle
    ligne avant la ligne sélectionnée.</li>
  <li><strong>Ajouter une ligne</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl n</code>)ajoute une nouvelle
    ligne après la ligne sélectionnée.</li>
</ul>

<p>Si on a sélectionné une ligne complète par <code>F2</code> (Windows) ou
<code>Esc</code> (Unix) la touche Enter crée une nouvelle ligne après.</p>

<h3>Détruire une ligne</h3>

<p>Pour détruire une ligne de table, il faut sélectionner la ligne et
utiliser la commande <code>Delete</code>. Il y a deux méthodes pour
sélectionner une ligne :</p>
<ol>
  <li>Soit l'entrée <strong>Sélectionner la ligne</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl r</code>).</li>
  <li>Soit poser la sélection sur une cellule de la ligne (Cell 1.1 dans
    l'exemple ci-dessous) puis deux fois <code>F2</code> (Windows) ou
    <code>Esc</code> (Unix) pour sélectionner la ligne.</li>
</ol>

<h3>Copier/Coller ou Déplacer une ligne</h3>

<p>Pour déplacer ou copier/coller une ligne de table :</p>
<ol>
  <li>Il faut sélectionner la ligne comme dans le cas de <strong>Détruire une
    ligne</strong> et copier (<code>Ctrl c</code>) ou couper (<code>Ctrl
    x</code> pour Windows, <code>Ctrl w</code> pour Unix) son contenu.</li>
  <li>Pour coller la ligne avant une autre ligne
    <ul>
      <li>soit on sélectionne le début d'une ligne et on active la commande
        coller (<code>Ctrl v</code> pour Windows, <code>Ctrl y</code> pour
        Unix),</li>
      <li>soit on utilise l'entrée de menu <strong>Coller avant</strong> dans
        le sous-menu <strong>Table</strong> (<code>Ctrl t Ctrl p</code>).</li>
    </ul>
  </li>
  <li>Pour coller la colonne après une autre colonne
    <ul>
      <li>soit on sélectionne la fin d'une ligne et on active la commande
        coller (<code>Ctrl v</code> pour Windows, <code>Ctrl y</code> pour
        Unix),</li>
      <li>soit on utilise l'entrée de menu <strong>Coller après</strong> dans
        le sous-menu <strong>Table</strong> (<code>Ctrl t Ctrl v</code>).</li>
    </ul>
  </li>
</ol>

<h3>Cellules étendues</h3>

<p>Il n'existe quatre commandes spécifiques pour étendre ou réduire
l'extension de cellules :</p>
<ul>
  <li><strong>Joindre avec la cellule à droite</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl e</code>) étend la cellule
    d'une position à droite et colle le contenu des deux cellules.</li>
  <li><strong>Joindre avec la cellule au-dessous</strong> dans le sous-menu
    <strong>Table</strong> (<code>Ctrl t Ctrl j</code>)étend la cellule d'une
    position vers le bas et colle le contenu des deux cellules.</li>
  <li><strong>Réduire extension horizontale</strong> (<code>Ctrl t Ctrl
    s</code>) réduit l'extension horizontale de la cellule d'une position et
    engendre une cellule vide.</li>
  <li><strong>Réduire extension verticale</strong> (<code>Ctrl t Ctrl
    m</code>) réduit l'extension verticale de la cellule d'une position et
    engendre une cellule vide.</li>
</ul>

<p>On peut aussi éditer directement les attributs colspan et rowspan de la
cellule.</p>

<h3>Ajouter un nouveau corps de table (tbody)</h3>
<ol>
  <li>Pour créer un nouveau corps de table en dessous d'un corps de table
    existant, il faut placer le curseur à la fin de la dernière cellule de la
    dernière ligne du corps de table et sélectionner l'entrée <strong>Corps
    (tbody)</strong> dans le sous-menu <strong>Table</strong>.</li>
  <li>Pour créer un nouveau corps de table au-dessus d'une ligne existante,
    il faut placer le curseur au début de la première cellule de la ligne et
    sélectionner l'entrée <strong>Ligne</strong> dans le sous-menu
    <strong>Table</strong>.</li>
</ol>

<p>De nouveaux corps de table peuvent être créés efficacement avec la touche
<a href="Creating.html.fr#L906">Enter</a>. Quand le corps de table entier est
sélectionné (il faut utiliser la touche <code>F2</code> sous Windows et
<code>Esc</code> sous Linux pour ça), en tapant la touche <code>Enter</code>
vous créez une nouveau corps de table après le corps de table sélectionné.</p>

<h3><a id="Adding21">Ajouter quelque chose après une table</a></h3>

<p>De nombreux types d'éléments peuvent être inclus à l'intérieur des
cellules de la table. Pour cette raison, Amaya pourrait mal comprendre vos
intentions quand vous sélectionnez un type d'élément (par exemple cliquant
H1, H2, etc. ou sélectionnant une entrée dans le menu <strong>Xhtml</strong>)
pendant qu'un composant de la table est sélectionné.</p>

<h3>Création d'un paragraphe après une table</h3>

<p>Quand la sélection se trouve dans une table, la touche <code>Enter</code>
crée un nouveau paragraphe dans la table.</p>

<p>Pour créer un paragraphe après la table, il faut sélectionner la table
entière en utilisant la touche <code>Esc</code> (Unix) ou <code>F2</code>
(Windows) puis <code>Enter</code>.</p>

<p>Ce nouveau paragraphe peut être modifié si besoin est.</p>

<p></p>

<p class="exercice"><em>Exercice 4 :</em> Créer une table, créer une ligne
avant/après, déplacer une ligne, créer une colonne avant/après, déplacer une
colonne.</p>
</div>

<div>
<h2 id="L12281">10. Recherche/Remplacement - Correction orthographique</h2>

<h3>Recherche et remplacement de texte</h3>

<p>La recherche/remplacement se lance, soit en cliquant sur le bouton <img
src="../images/find.gif" alt="find " />, soit par l'entrée de menu
<strong>Edition/Rechercher</strong>. Un boite de dialogue apparaît. vous avez
deux zones de saisie la première sera pour le texte à rechercher la deuxième
zone de saisie sera pour le texte à remplacer.</p>

<p style="text-align: center"><img alt="(Rechercher/Remplacer)"
src="../images/search_and_replace-fr.png" /></p>

<p>Pour chacune des actions vous avez des choix d'options.</p>
<ul>
  <li>les options de recherche :
    <ul>
      <li>la casse : pour la recherche permet de spécifier s'il y a lieu de
        différencier ou non les majuscules des minuscules.</li>
      <li>case à cocher pour sélectionner la zone sur laquelle réaliser la
        recherche avant la sélection, après la sélection, dans la sélection
        ou sur la totalité du document.</li>
    </ul>
  </li>
  <li>Les options de remplacement :
    <ul>
      <li>le type de remplacement : pas de remplacement (juste une
        recherche), remplacement avec un fenêtre de confirmation,
        remplacement automatique.</li>
    </ul>
  </li>
</ul>

<h3>Correction orthographique</h3>

<p>La correction orthographique se réalise à l'aide d'une boite de dialogue
activée par l'action <strong>Edition/Vérifier orthographe</strong>.</p>

<p style="text-align: center"><img alt="(Correcteur)"
src="../images/spellchecking-fr.png" /></p>

<p>les options de la boite de dialogue :</p>
<ul>
  <li>Le dictionnaire utilisé se fait en fonction de la langue définie au
    niveau de l'élément ou du document.</li>
  <li>On peut spécifier le nombre de propositions (par défaut 3).</li>
  <li>On peut exclure des éléments lors de recherche de correspondance de
    mots avec le dictionnaire (par défaut toutes les options sont activé)</li>
  <li>Options de zone de recherche vous avez la possibilité de spécifier la
    zone sur laquelle cette correction orthographique se fera avant la
    sélection, après la sélection, dans la sélection ou sur la totalité du
    document.</li>
  <li>les boutons d'activation de recherche.
    <ul>
      <li>recherche du suivant (sans action) : considère que le mot est
        correcte et passe à l'erreur suivante.</li>
      <li>rajoute au dictionnaire personnel : rajoute le mot au dictionnaire
        personnel.</li>
      <li>remplace le mots et passe à l'occurrence suivante.</li>
      <li>remplace le mots et le rajoute dans le dictionnaire personnel.</li>
      <li>confirmer permet de fermer la boite de dialogue.</li>
    </ul>
  </li>
</ul>

<p class="exercice"><em>Exercice 5 :</em> Rechercher/remplacer, changer la
lange d'un passage et passer le correcteur orthographique.</p>
</div>

<div>
<h2 id="L1230">11. Attributs</h2>

<p>Il y a deux manières d'éditer des attributs :</p>
<ul>
  <li>Le menu <strong>Attributs</strong></li>
  <li>La Vue structure</li>
</ul>

<h3><a id="L1073">Editer des attributs par le biais du menu Attributs</a></h3>

<p>Le menu <strong>Attributs</strong> donne une liste de tous les attributs
qui peuvent être associés à l'élément sélectionné. Il peut être utilisé pour
créer un attribut, pour changer la valeur d'un attribut existant ou pour
effacer un attribut.</p>

<p>Pour éditer un attribut, sélectionnez d'abord l'élément approprié. Vous
devez sélectionner l'élément entier et pas seulement un caractère à
l'intérieur de l'élément. Pour cela, il faut utiliser la touche
<code>F2</code> (Windows) ou <code>Esc</code> (Linux). Quand l'élément est
sélectionné, le menu <strong>Attributs</strong> affiche la liste des
attributs applicables. On peut voir rapidement si l'attribut a une valeur ou
non</p>

<h3><a id="Editing">Editer des attributs dans la Vue structure</a></h3>

<p>Tous les attributs sont affichés dans la Vue structure. Les attributs dont
la valeur peut être choisie librement sont affichés en prune et vous pouvez
éditer leur valeur comme n'importe quelle autre chaîne de caractères. Quand
la valeur de l'attribut est affichée en bleu, vous pouvez seulement la
changer à l'aide du menu <strong>Attributs</strong>. Ceci assure que seules
des valeurs correctes soient choisies.</p>

<p>Quand la sélection se trouve dans une valeur d'attribut, la touche
<code>F2</code> (Windows) ou <code>Esc</code> (Linux) sélectionne la valeur
complète. La touche <code>Delete</code> détruit alors la valeur, et un
deuxième <code>Delete</code> supprimera l'attribut.</p>
</div>

<div>
<h2 id="L1232">12. Création de feuilles de style CSS</h2>

<h3><a id="feuilles">Les feuilles de style CSS externes et les feuilles de
style CSS utilisateur</a></h3>

<p>Un document peut associer plusieurs feuilles de style externes qui sont
chargées avec le document et lui sont appliquées. Une feuille de style
externe peut s'appliquer à plusieurs documents.</p>

<p>L'utilisateur peut définir une feuille de style spécifique qui s'applique
à tous les documents chargés. Cette feuille de style est appelée
<strong>Feuille de Style Utilisateur</strong> et est le fichier local
<strong>amaya.css</strong> conservé dans l'environnement de l'utilisateur (le
<a href="Configure.html.fr#AmayaHome">répertoire d'AmayaHome</a>). Par
exemple si vous préférez afficher les document en Helvetica et imprimer avec
une police de caractères de petite taille, vous pouvez créer votre feuille de
style utilisateur avec les règles CSS suivantes :</p>
<pre>@media print {
 BODY {font-size: 10pt}
}
@media screen {
 BODY {font-family: Helvetica}
}</pre>

<h3><a id="Managing">Gérer des feuilles de style CSS externes</a></h3>

<p>L'entrée de feuilles de style donne accès à une série de commandes qui
permettent de contrôler les feuilles de style CSS externes et celles qui
donnent les préférences de l'utilisateur :</p>
<ul>
  <li>La commande <strong>Ajouter</strong> permet de lier une nouvelle
    feuille de style CSS à un document courant. On peut cliquer directement
    sur la cible de la feuille de style si elle est déjà visualisée dans la
    fenêtre Amaya. Si ce n'est pas le cas, l'utilisateur devra cliquer dans
    le document courant pour avoir accès à une boîte de dialogue et taper
    l'URL de la cible de la feuille de style. Cette commande ajoute un
    élément lien avec ses attributs à la tête du document.</li>
  <li>La commande <strong>Ouvrir</strong> permet d'ouvrir une des feuilles de
    style CSS appliquées au document courant.</li>
  <li>La commande <strong>Désactiver</strong> permet de supprimer les effets
    d'une des feuilles de style actives du document courant. Cette commande
    n'affecte en rien le lien CSS dans le document.</li>
  <li>La commande <strong>Réactiver</strong> permet de rétablir les effets
    d'une des feuilles de style CSS désactivées du document courant. Cette
    commande n'affecte en rien le lien CSS dans le document.</li>
  <li>La commande <strong>Retirer</strong> permet de retirer une des feuilles
    de style CSS externes. Cette commande agit comme la commande Désactiver.
    En plus de cela, l'élément lien qui attache les feuilles de style CSS est
    supprimé.</li>
</ul>

<p>Les commandes Ouvrir, Désactiver et Réactiver peuvent être appliquées aux
feuilles de style qui donnent les préférences de l'utilisateur, mais les
commandes Ajouter et Retirer concernent uniquement les feuilles de style
reliées. La figure suivante montre la liste des feuilles de style CSS
proposées pour la commande Ouvrir. Si aucune feuille de style CSS n'est
actuellement appliquée à un document, une boîte de dialogue expliquera
"qu'aucun fichier CSS n'est disponible".</p>

<p><img alt="css.gif" src="../images/css.gif" /></p>

<p></p>

<h3>Visualiser le style appliqué</h3>

<p>S'il y a beaucoup de feuilles de style attachées à un document, il est
difficile de comprendre pourquoi un élément est visualisé avec cette couleur
ou ces marges, etc. Pour aider l'utilisateur, Amaya fournit une commande
<strong>Montrer le style appliqué</strong> qui visualise dans une fenêtre
externe les propriétés CSS appliquées au premier élément de la sélection
courante.</p>

<p>L'utilisateur peut afficher le source CSS qui engendre chaque propriété en
double (ou simple) cliquant sur la propriété. Aussi longtemps que la fenêtre
est ouverte, Amaya met à jour le contenu de la fenêtre quand la sélection
change.</p>

<p>En appuyant sur la touche <code>F2</code> (ou <code>Esc</code> sur les
plates-formes Linux), l'utilisateur peut voir les propriétés CSS appliquées
aux éléments englobants.</p>

<h3>Créer des propriétés CSS</h3>

<p>Amaya ne fournit pas pour le moment une liste complète des propriétés CSS
2 sauf pour les propriétés les plus courantes. En général l'utilisateur doit
donc se reporter à la spécification CSS 2 pour connaître le nom des
propriétés et leurs valeurs.</p>

<p>Les quatre premières entrées du menu <strong>Style</strong> permettent de
produire les propriétés CSS les plus courantes pour les éléments sélectionnés
:<strong></strong><strong></strong></p>
<ul>
  <li><h3><span style="font-size: 12pt"><span
    style="font-weight: normal">L'option</span> Style de caractères <span
    style="font-weight: normal">permet de choisir certaines polices de
    caractère, le style du caractère, la taille du corps et le style
    souligné.</span></span></h3>
  </li>
</ul>
<ul>
  <li><strong><span style="font-weight: normal">L'option</span>
    Couleurs</strong> permet de choisir les couleurs du premier plan et de
    l'arrière plan dans la palette. Cliquez sur une cellule de la palette
    avec le bouton gauche de la souris pour attribuer la couleur du premier
    plan, cliquez sur le bouton du milieu pour attribuer la couleur de
    l'arrière plan.</li>
  <li>L'option <strong>Format</strong> permet de choisir l'alignement, le
    renfoncement, la justification et l'interligne.</li>
  <li>L'option <strong>Image de fond</strong> permet de choisir une image de
    fond et son mode de répétition.</li>
</ul>

<p>Pour le moment le style est associé à un ou plusieurs éléments
particuliers. Un élément peut être utilisé comme un modèle pour créer ou
modifier une règle de style :</p>
<ul>
  <li>On sélectionne cet élément (un seul élément ayant un attribut
    <code>style</code>)</li>
  <li>On sélectionne l'entrée <strong>Créer une règle</strong> du menu
    <strong>Style</strong>. Une boîte de dialogue surgit et visualise une
    liste de sélecteurs CSS. Cette liste inclus toutes les classes qui sont
    définies pour le document courant (y compris les classes définies par les
    feuilles de style liées au document), ainsi que les types d'élément
    auquel le style est associé.</li>
  <li>On choisit un sélecteur dans cette liste ou on saisit un nouveau nom de
    classe puis on confirme. Le style de l'élément sélectionné est désormais
    associé au sélecteur choisi et l'attribut <code>style</code> est supprimé
    pour l'élément sélectionné. Son contenu est déplacé à l'élément
    <code>STYLE</code>, dans le <code>HEAD</code> document, et tous les
    éléments correspondant au sélecteur sont visualisés dans le nouveau
  style.</li>
  <li>Si une nouvelle classe est créée, elle peut être appliquée à d'autre
    éléments du document  par la commande <strong>Appliquer une
    classe</strong> du menu <strong>Style</strong>. Une boîte de dialogue
    surgit, affichant toutes les classes existantes. On choisit une de ces
    classes. Les éléments sélectionnés sont désormais visualisés avec le
    style associé à la classe choisie.</li>
</ul>

<p>Pour associer une classe à un élément, vous pouvez également utiliser le
<a href="Attributes.html.fr#L1073">menu <strong>Attributs</strong></a>. Ce
menu permet aussi d'effacer un attribut de classe pour un élément ou de le
changer : l'attribut de classe se manipule comme un autre attribut.</p>

<p class="exercice"><em>Exercice 6 :</em> Mettre de la couleur, appliquer une
feuille de style, voir le style.</p>
</div>

<div>
<h2 id="L1242">13. Détection des erreurs</h2>

<p>Au chargement d'un document, Amaya peut détecter des erreurs de HTML (mais
le parseur HTML est très tolérant) ou des incompatibilités avec la famille
XHTML choisie.</p>
</div>
<dl>
  <dt>1er exemple :</dt>
    <dd>Un document est déclaré comme HTML 4.0 Strict et contient des
      attributs non acceptés.
      <p><img alt="(profil invalide)" src="../images/invalid_profile-fr.png"
      /></p>
    </dd>
  <dt>2ème exemple :</dt>
    <dd>Un document XHTML mal formé
      <p><img alt="(mal formé)" src="../images/not_well_formed-fr.png" /></p>
    </dd>
</dl>

<div>
<h4>On peut corriger les erreurs</h4>

<p>On peut voir le détail des erreurs détectées dans l'entrée <strong>Montrer
les erreurs</strong> du menu <strong>Vues</strong>. Pour corriger ces
erreurs, on active le lien vers la vue source du document pour visualiser la
position de l'erreur dans le source.</p>

<p>Une fois les erreurs corrigées on synchronise pour voir si tout est
correct, on peut alors sauver le document corrigé.</p>

<h4>On peut aussi modifier le type du document</h4>

<p>L'entrée <strong>Modifier le doctype</strong> du menu
<strong>Fichier</strong> permet de modifier le type du document.</p>

<p>Dans le 2ème exemple, le bouton <strong>Recharger</strong> permet de lire
le document comme un document HTML au lieu de XHTML et donc éviter de bloquer
sur le parseur XML.</p>
</div>

<p></p>
</body>
</html>
